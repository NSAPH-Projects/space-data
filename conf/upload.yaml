defaults:
  - _self_
  - space_dataverse.yaml

base_name: ???
debug: false  # when debug is true, the script will not upload to dataverse

hydra:
  run:
    dir: uploads/${base_name}
  job:
    chdir: true
